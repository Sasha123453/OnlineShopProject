@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<p id="answer"></p>
<button id="sendConfirmation">Отправить подтверждение на почту</button>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    async function checkEmail(){
        var res = await fetch("/identity/personal/isemailconfirmed",
            {
                method: "POST",
                headers: {
                    "Accept": "application/json"
                }
            });
        var x = await res.json();
        debugger;
    }
    $(document).ready(checkEmail);
    async function sendConfirmation() {
        var res = await fetch("/identity/auth/sendemail", {
            method: "GET"
        })
        if (res.ok) console.log("email sent!");
    }
    var button = document.getElementById("sendConfirmation");
    button.addEventListener("click", sendConfirmation);
</script>